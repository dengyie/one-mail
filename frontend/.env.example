# 前后端分离（唯一形态）：VITE_API_BASE 指向后端 Worker 根地址。
# 生产必须填 https:// 开头、末尾不带 / 的 Worker 自定义域名。
# Pages 代理（PAGE_TOML 反代、"空 VITE_API_BASE 走同域"）形态已废弃。
VITE_API_BASE=https://temp-email-api.xxx.xxx
VITE_CF_WEB_ANALY_TOKEN=
VITE_IS_TELEGRAM=false
